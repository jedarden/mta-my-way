Bead closed successfully. Here's a summary of what was done:

**Task: bf-3dj0 — Cancelled trip detection via NYCT trip_replacement_period**

The implementation was already complete from prior work on the branch. All four layers were in place:

1. **`parser.ts`** — Exports `TripReplacementWindow` interface and extracts `tripReplacementPeriods[]` from the NYCT feed header extension, handling protobufjs Long values
2. **`transformer.ts`** — Cross-poll state tracking via `prevTripStops` map; trips absent from the current feed within their route's replacement window are emitted as `ArrivalTime` with `isCancelled: true`, `isAssigned: false`, `confidence: "low"`
3. **`arrivals.ts`** — `isCancelled?: boolean` field on `ArrivalTime`
4. **`ArrivalRow.tsx`** — Struck-through time, `opacity-50` dimming, and red CNCL badge with tooltip

Test fixtures (`bDivisionFeedMissingFTrip`) and 4 transformer tests covering the detection, inactive-feed suppression, and false-positive prevention all pass (41/41).

Committed 6 files (392 insertions), pushed to remote, bead closed.
