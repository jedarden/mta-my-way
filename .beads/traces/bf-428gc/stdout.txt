Done. Here's a summary:

**Bead bf-428gc closed.** The debug workflow `mta-my-way-lint-capture-p5qwp` was identified, and the lint step pod name was `mta-my-way-lint-capture-p5qwp-lint-1987986433` (Succeeded). The pods have since been garbage-collected by the TTL strategy. The pod naming pattern across all mta-my-way builds is `<workflow>-<templateName>-<nodeId>`, so lint pods always follow `<workflow>-lint-<nodeId>`.
