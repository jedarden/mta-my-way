Bead closed. Summary:

- **84/84 middleware chain integration tests pass** — all green, deterministic, no flakes
- **Full test suite passes** (5073 tests across 202 files)
- **Test file lints clean** (zero biome errors)
- **No code changes needed** — the tests were already stable and covering all acceptance criteria: middleware ordering, security headers on all responses, happy-path auth flow, failure-path (missing auth → 401/403, invalid CSRF → 403, rate limits → 429), and audit logging
