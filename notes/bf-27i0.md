# bf-27i0: Fix type-only import bug for IP_A and IP_B

**Status:** Already completed in commit d27f204

## Summary

The fix for the type-only import bug was already applied on 2026-07-02. The commit message states:

> "fix: import IP_A and IP_B as values not types in audit log tests
>
> The rate-limiter-harness exports IP_A and IP_B as runtime string constants,
> but audit-log-security-events.test.ts imported them with  which erases
> them at compile time, causing ReferenceError at runtime. Removed type-only
> import to fix all 22 failing tests (15 now pass, remaining failures are
> unrelated middleware behavior issues)."

## Current State

The file `packages/server/src/integration/audit-log-security-events.test.ts` has the correct imports on lines 35-36:

```typescript
import {
  type AuthVars,
  IP_A,      // ✓ value import (correct)
  IP_B,      // ✓ value import (correct)
  disableRateLimiting,
  enableRateLimiting,
  ...
} from "../test/rate-limiter-harness.js";
```

## Verification

IP_A and IP_B are exported as runtime constants in `rate-limiter-harness.ts`:
```typescript
export const IP_A = "10.0.0.1";
export const IP_B = "10.0.0.2";
```

They are now correctly imported as values in the test file, not as type-only imports.
