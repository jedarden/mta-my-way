# Bead bf-445l: Long-press on Line Bullet - Already Implemented

## Task Verification

The long-press functionality on line bullets was already fully implemented in the codebase.

## Implementation Location

`packages/web/src/components/arrivals/LineBullet.tsx`

## What Was Verified

1. **Long-press gesture**: 300ms hold timer using pointer events
   - `onPointerDown` starts the timer
   - `onPointerUp`, `onPointerLeave`, `onPointerCancel` clear the timer

2. **Tooltip content** (matches plan section 5.2):
   - Full line name (e.g., "Broadway-7th Ave Local")
   - Division (A or B)
   - Express/Local classification

3. **Behavior**:
   - Tooltip appears after 300ms hold
   - Hides on pointer up or leave
   - Click is prevented after long press (prevents accidental navigation)
   - Context menu is prevented on long-press

4. **Test coverage**: 36 tests, all passing
   - Tests verify tooltip timing, content, and behavior
   - Edge cases covered (unknown lines, early release, etc.)

## Conclusion

No implementation work was required. The feature was already complete.
